import React from "react";

export function titleTeks(value) {
  return <span>{value}</span>;
}
export function titleLogo(value) {
  return (
    <div>
      {/* its must Images  Icons */}
      <span>{value}</span>
    </div>
  );
}
